/** led.h
 * 仕様は一旦保留のため空にしておきます
 */

#ifndef _LED_H_
#define _LED_H_

#if defined(ARDUINO) && ARDUINO >= 100
#include "Arduino.h"
#else
#include "WProgram.h"
#endif

#include <Adafruit_NeoPixel.h>

#endif